# Copy to .env.local for local development — leave blank to use the Vite dev proxy
VITE_API_URL=

# Example for production (set this in Vercel environment variables, not here)
# VITE_API_URL=https://your-app.up.railway.app/api
